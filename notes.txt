ls
pwd
mkdir "directoryname"   
cd "directoryname"
touch "filename"
git status
git add . (git add "filename")
git log
git commit -m "message"
git checkout "commit Id" / main 
git restore --staged "filename"

git checkout -b "branchname"
git branch "branchname"
git branch -d "branchname"

git merge "branchname"

git remote add origin "https://github.com/Divyansh0702/Git-Session.git"
git remote -v